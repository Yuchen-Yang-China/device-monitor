# Windows Monitor

此目录预留给 Device Monitor 的 Windows 托盘应用。

在 Windows 设备上克隆本仓库后，将现有 Windows 工程文件放入本目录；不要创建第二个 `.git` 目录，也不要修改 Peer Status Protocol v1 的字段名、单位、签名格式或默认端口。

开始前请阅读仓库根目录的 `docs/WINDOWS_CODEX_HANDOFF.zh-CN.md`。应用图标位于 `shared/assets/windows/DeviceMonitor.ico`。
